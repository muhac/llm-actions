name: LLM smoke test

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  infer:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - model: "Qwen/Qwen3.5-0.8B"
          - model: "Qwen/Qwen3.5-2B"
          - model: "Qwen/Qwen3.5-4B"
            kv_cache_space: "4"
            max_model_len: "32768"
          - model: "Qwen/Qwen2.5-1.5B-Instruct"
          - model: "Qwen/Qwen2.5-VL-3B-Instruct"
          - model: "Qwen/Qwen3-1.7B"
          - model: "Qwen/Qwen3-4B"
            kv_cache_space: "4"
            max_model_len: "29056"

    steps:
      - uses: actions/checkout@v4

      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@main
        with:
          large-packages: false
          swap-storage: false

      - name: Run LLM inference
        id: run_llm
        uses: ./
        with:
          model: ${{ matrix.model }}
          kv_cache_space: ${{ matrix.kv_cache_space }}
          max_model_len: ${{ matrix.max_model_len }}

      - name: Show vLLM logs
        env:
          VLLM_LOGS: ${{ steps.run_llm.outputs.logs }}
        run: echo "$VLLM_LOGS"

      - name: List files
        run: tree -a -L 3 ..

      - name: Test the server
        run: curl --retry 5 --retry-delay 10 http://localhost:8000/v1/models

      - name: Demo chat
        run: |
          curl http://localhost:8000/v1/chat/completions \
          -o response.json \
          -H "Content-Type: application/json" \
          -d '{
              "model": "${{ matrix.model }}",
              "messages": [
                  {"role": "system", "content": "You are a teacher."},
                  {"role": "user", "content": "How many times does the letter R appear in the word strawberry?"}
              ]
          }'

      - name: Show response
        run: cat response.json | jq . || cat response.json

      - name: Demo chat (Chinese)
        run: |
          curl http://localhost:8000/v1/chat/completions \
          -o response.json \
          -H "Content-Type: application/json" \
          -d '{
              "model": "${{ matrix.model }}",
              "messages": [
                  {"role": "user", "content": "陨石为什么每次都能精准砸到陨石坑？"}
              ]
          }'

      - name: Show response
        run: cat response.json | jq . || cat response.json

      - name: Demo chat (no_think)
        run: |
          curl http://localhost:8000/v1/chat/completions \
          -o response.json \
          -H "Content-Type: application/json" \
          -d '{
              "model": "${{ matrix.model }}",
              "messages": [
                  {"role": "system", "content": "/no_think"},
                  {"role": "user", "content": "How many times does the letter R appear in the word strawberry?"}
              ]
          }'

      - name: Show response
        run: cat response.json | jq . || cat response.json

      - name: Demo chat (image)
        continue-on-error: true
        run: |
          curl http://localhost:8000/v1/chat/completions \
          -o response.json \
          -H "Content-Type: application/json" \
          -d '{
              "model": "${{ matrix.model }}",
              "messages": [
                  {
                      "role": "user",
                      "content": [
                          {"type": "image_url", "image_url": {"url": "https://qianwen-res.oss-cn-beijing.aliyuncs.com/Qwen-VL/assets/demo.jpeg"}},
                          {"type": "text", "text": "Describe this image."}
                      ]
                  }
              ]
          }'

      - name: Show response
        run: cat response.json | jq . || cat response.json
